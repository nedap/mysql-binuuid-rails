language: ruby
cache: bundler
rvm:
  - 2.2.7
  - 2.3.4
  - 2.4.1
env:
  matrix:
    - RAILS_VERSION=5.0.4
    - RAILS_VERSION=5.1.0
    - RAILS_VERSION=5.1.1
    - RAILS_VERSION=5.1.2
before_install:
  - gem update bundler
  - bundle update rails
