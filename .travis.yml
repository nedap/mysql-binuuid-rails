language: ruby
cache: bundler
rvm:
  - 2.4.5
  - 2.5.3
  - 2.6.2
env:
  matrix:
    - RAILS_VERSION=5.0.5
    - RAILS_VERSION=5.1.6
    - RAILS_VERSION=5.2.0
    - RAILS_VERSION=5.2.1
    - RAILS_VERSION=5.2.2
    - RAILS_VERSION=5.2.3
before_install:
  - gem install bundler
  - gem update bundler
  - bundle update
