language: ruby
cache: bundler
rvm:
  - 2.3.8
  - 2.4.5
  - 2.5.3
env:
  matrix:
    - RAILS_VERSION=5.0.5
    - RAILS_VERSION=5.1.6
    - RAILS_VERSION=5.2.0
    - RAILS_VERSION=5.2.1
before_install:
  - gem install bundler
  - gem update bundler
  - bundle update
